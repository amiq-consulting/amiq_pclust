To ensure python is correctly set up for this module, use the commands:

$ python3 -m venv venv
$ source venv/bin/activate
$ pip install --upgrade pip
$ pip install -U -r py_requirements.txt

To use the Matrix Determinant sandbox set an environment variable as follows:

$ export PROJ_HOME="path/to/amiq_pclust/amiq23_md/"

