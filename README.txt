To ensure python is correctly set up for this module, use the commands:

python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -U -r py_requirements.txt

To use the MD example, proj home must be set as follows:

export PROJ_HOME="~/$USER/git/amiq_pclust/amiq23_md/"
